This automatically deploys metalsmith html page to github pages.

In order to deploy the site, edit the markdown in src/
Run deploy.sh, which should deploy the markdown to html in build/, while pushing to the master branch